import { createContext } from "react";

export const AuthContext = createContext();

// npm run build
// _redirects
// firebase e project er authentication er setting e giye netlify er domain add app porjonto